# 手机远程连接电视 App 方案设计（手机端 + 电视端，无中心服务器版）

## 1. 产品目标

打造一套「手机异地控制电视」的双端应用：
- **手机端（iOS/Android）**：作为遥控器、输入设备、投屏入口。
- **电视端（Android TV / 自研 TV OS）**：作为被控设备，主动维护外网可访问地址（固定域名 + 动态 IP）。

核心目标：
1. 不依赖中心业务服务器（No Backend）。
2. 在异地网络（非同一 Wi-Fi）下可直接连接控制电视。
3. 使用固定域名访问电视端，电视 IP 变化时自动更新解析。
4. 保留局域网内低延迟控制体验。

---

## 2. 典型用户场景

1. **同局域网遥控**：手机直接发现电视并控制。
2. **异地遥控**：用户在公司/外出时，使用绑定域名连接家中电视。
3. **文字输入**：手机键盘输入电视搜索内容。
4. **远程启动应用**：手机一键打开电视端目标应用。
5. **远程媒体播放**：向电视推送可访问媒体 URL 并远程控制。
6. **反向看屏（重点新增）**：手机实时查看电视当前画面，类似投屏预览/远程看屏。

---

## 3. 功能架构

## 3.1 手机端功能模块

- **双通道连接模块（LAN/WAN）**
  - LAN：mDNS/SSDP 自动发现。
  - WAN：基于用户录入或扫码保存的电视固定域名（如 `tv-home.example.net`）直连。

- **配对与信任模块**
  - 首次配对：PIN 码 + 设备公钥指纹确认。
  - 保存电视域名、端口、证书指纹，后续直连。

- **遥控器模块**
  - 方向、确认、返回、主页、音量、静音等。
  - 长按/连发机制（如音量连续加减）。

- **输入桥接模块**
  - 手机文本输入、粘贴、删除、提交。

- **远程看屏播放器模块（新增）**
  - 接收电视端推送的视频流（H.264/H.265）。
  - 支持清晰度切换（流畅/高清）、静音播放、全屏手势。
  - 支持“仅预览关键帧模式”以降低带宽消耗。

- **连接诊断模块**
  - 显示当前通道（LAN/WAN）、RTT、失败原因（DNS 失败、端口不可达、证书不匹配）。

## 3.2 电视端功能模块

- **控制服务守护进程**
  - 开机自启，监听本地控制端口（例如 `7443`）。

- **动态 IP 绑定模块（核心新增）**
  - 集成 DDNS 客户端。
  - 周期检测公网 IP（IPv4/IPv6）变化。
  - IP 变化后自动向 DDNS 提供商更新 A/AAAA 记录。
  - 提供状态页：当前绑定域名、最近更新时间、更新结果。

- **NAT/端口可达性检测模块**
  - 检测 UPnP/NAT-PMP 是否可自动映射端口。
  - 若失败，提示用户在路由器手动端口转发。

- **指令执行层**
  - 命令映射到系统按键和应用行为。

- **屏幕采集与编码模块（新增）**
  - 基于系统能力采集电视画面（MediaProjection/厂商能力）。
  - 实时编码 H.264/H.265，支持分辨率/码率自适应。
  - 音频默认关闭，可配置仅传画面（更安全、更省带宽）。

- **安全模块**
  - 配对凭据、设备白名单、会话超时、异常来源 IP 拦截。

---

## 4. 无服务器异地连接架构（重点）

## 4.1 架构原则

1. **无中心控制服务器**：手机直接连接电视域名。
2. **电视端主动维护域名解析**：电视 App 内置 DDNS 更新逻辑。
3. **端到端加密与设备认证**：即便公开网络也保证会话安全。

## 4.2 网络拓扑

- 手机端（任意网络）
  ↓
- DNS 解析 `tv-home.example.net` → 当前家庭公网 IP
  ↓
- 路由器端口转发（公网 `7443` -> 电视内网 `7443`）
  ↓
- 电视端控制服务（WSS/QUIC）

> 注：这里仅依赖 DNS 基础设施与 DDNS 服务，不依赖你自己的业务后端。

## 4.3 动态 IP 绑定实现建议

- **方案 A（推荐）**：集成标准 DDNS 提供商 API（Cloudflare、DuckDNS、阿里云解析等）。
- **方案 B**：用户使用路由器 DDNS；电视 App 仅做连通性检测与引导。
- **方案 C**：双保险，路由器 DDNS + 电视 App DDNS 都可配置，任一成功即可。

电视端 App 需提供：
- DDNS 提供商选择
- 域名、Token/Key 配置
- 更新频率（默认 5~10 分钟 + IP 变更触发）
- 更新重试策略（指数退避）
- 更新日志可视化

## 4.4 协议建议

- **控制通道**：WSS（WebSocket over TLS）优先。
- **看屏媒体通道（新增）**：WebRTC（推荐）或基于 QUIC 的低延迟流媒体。
- **低延迟场景**：控制与媒体分离，控制走 WSS，视频走 WebRTC。
- **状态同步**：JSON 事件 + 心跳（10~20 秒）。

---

## 5. 关键流程设计

## 5.1 首次异地可用配置流程

1. 电视端打开“异地控制设置”。
2. 用户配置 DDNS（域名 + Token）。
3. 电视端检测公网 IP 并更新解析记录。
4. 电视端检测端口可达性（UPnP 或手动映射）。
5. 手机扫码保存连接信息（域名、端口、设备指纹）。
6. 手机发起配对，电视显示 PIN，完成互信绑定。

## 5.2 日常异地控制流程

1. 手机选择家庭电视。
2. 解析域名并连接 `wss://tv-home.example.net:7443`。
3. 双向 TLS + 设备签名校验。
4. 手机发送遥控命令，电视执行并回传状态。

## 5.3 远程看屏流程（手机看电视画面）

1. 手机连接电视控制通道后，点击“查看电视画面”。
2. 手机发起 `start_screen_stream` 请求（含期望分辨率/帧率）。
3. 电视端启动采集与编码，建立 WebRTC 会话。
4. 手机端播放器实时渲染画面，并可继续发送遥控指令。
5. 网络变差时自动降级到低码率或关键帧预览模式。

## 5.4 IP 变更自动恢复流程

1. 宽带重拨导致公网 IP 变化。
2. 电视端 DDNS 模块检测变化并更新解析。
3. 手机下次连接仍使用同一固定域名，无需修改配置。

---

## 6. 数据与接口设计（示例）

## 6.1 DDNS 更新状态消息

```json
{
  "type": "ddns_status",
  "payload": {
    "domain": "tv-home.example.net",
    "publicIp": "203.0.113.8",
    "lastUpdateTs": 1730000000,
    "provider": "cloudflare",
    "result": "ok"
  }
}
```

## 6.2 开启看屏消息体

```json
{
  "type": "start_screen_stream",
  "deviceId": "tv_001",
  "payload": {
    "videoCodec": "h264",
    "maxWidth": 1280,
    "maxFps": 20,
    "profile": "low_latency"
  }
}
```

## 6.3 控制指令消息体

```json
{
  "type": "remote_command",
  "deviceId": "tv_001",
  "ts": 1730000001,
  "payload": {
    "command": "KEY_HOME",
    "repeat": 1
  }
}
```

---

## 7. 安全与隐私

1. **TLS + 证书固定（Pinning）**：防止中间人攻击。
2. **首次配对校验设备指纹**：阻断假设备接入。
3. **最小开放端口**：仅开放控制端口，禁用无关公网服务。
4. **访问控制**：白名单设备 + 失败次数限流 + 黑名单。
5. **敏感信息本地加密存储**：DDNS Token、配对密钥使用系统安全区保存。

---

## 8. 性能与可用性目标（SLO）

- 异地控制指令延迟：**P95 < 300ms**（常见公网环境）。
- 远程看屏端到端延迟：**P95 < 800ms**（720p 流畅档）。
- DDNS 更新生效时间：**< 60s**（依赖 DNS TTL 设定）。
- DDNS 更新成功率：**> 99%**。
- 远程连接可用率：**> 99%**。

---

## 9. UI/UX 设计建议

- 手机端设备卡片标记：`局域网直连` / `异地域名连接`。
- 遥控页新增“看电视画面”按钮，进入低延迟预览页。
- 电视端新增“动态域名绑定”页面：
  - 域名
  - 当前公网 IP
  - 最近更新状态
  - 端口映射检测结果
- 当远程不可达时，给出明确修复建议：
  - DNS 未更新
  - 端口未映射
  - 证书不匹配

---

## 10. 迭代路线图（无服务器版本）

### MVP（4~6 周）
- 局域网控制
- 基础配对
- 电视端 DDNS 绑定
- 异地域名直连控制
- 端口可达性检测
- 远程看屏（720p/20fps 基础版）

### V1.1
- 多 DDNS 提供商适配
- 自动 UPnP 映射
- 远程输入体验优化
- 看屏自适应码率与弱网降级

### V1.2
- QUIC 优化弱网
- 远程媒体控制增强
- 更细粒度权限与审计日志

---

## 11. 风险与应对

1. **家庭网络为 CGNAT，无法公网入站**
   - 应对：提示“当前网络不支持纯 P2P 入站”；提供可选中继模式（未来扩展）。
2. **用户不会配置端口映射**
   - 应对：优先自动 UPnP，失败后图文引导。
3. **DNS TTL 导致短暂不可达**
   - 应对：降低 TTL（如 60 秒）并优化重连策略。
4. **公网暴露带来安全风险**
   - 应对：强制 TLS、白名单、限流、防爆破。
5. **远程看屏带宽占用高**
   - 应对：默认 720p/20fps，支持动态降码率和关键帧预览模式。

---

## 12. 团队分工建议

- 手机端：2 人（连接策略 + 交互）
- 电视端：2~3 人（控制服务 + DDNS + NAT 诊断）
- 测试：1 人（家庭网络环境矩阵）
- 产品/设计：各 1 人

该方案可在不建设中心后端前提下，实现“固定域名 + 动态 IP”异地控制。
